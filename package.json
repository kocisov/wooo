{
  "name": "redux-wooo",
  "version": "0.1.4",
  "description": "Add redux scaffolding into your folder",
  "main": "lib/index.js",
  "repository": {
    "url": "kocisov/wooo",
    "type": "git"
  },
  "bin": {
    "wooo": "lib/index.js"
  },
  "scripts": {
    "pretest": "yarn compile && node lib/index.js",
    "test": "mocha test.js",
    "prepublish": "yarn compile",
    "compile": "yarn prettier && babel src -d lib",
    "compile:with-minify": "babel src -d lib --minified",
    "prettier": "prettier --write {src,templates}/**/*.js --single-quote --tab-width 2 --trailing-comma es5"
  },
  "author": "kocisov",
  "license": "MIT",
  "dependencies": {
    "babel-polyfill": "^6.23.0",
    "babel-preset-stage-0": "^6.24.1",
    "chalk": "^1.1.3",
    "clear": "^0.0.1",
    "fs-extra": "^2.1.1",
    "yargs": "^7.0.2"
  },
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-env": "^1.2.2",
    "mocha": "^3.2.0",
    "prettier": "^1.1.0"
  }
}
